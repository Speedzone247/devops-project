[defaults]
inventory        = inventory/hosts.ini
remote_user      = tegbele
private_key_file = ~/.ssh/devops_lab
host_key_checking = False
roles_path       = roles

[privilege_escalation]
become        = False
become_method = sudo
become_user   = root
